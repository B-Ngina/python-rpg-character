# python-rpg-character
This Python script features a robust character creation system. It implements comprehensive data validation to enforce game balance (7-point cap) and uses string manipulation to generate dynamic, visual stat bars in the terminal.
